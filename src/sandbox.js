"use strict";

const GrapeQL = require("./server/GrapeQL");
const GrapeQLCoach = require("./parser/GrapeQLCoach");

console.log( GrapeQL );
console.log( GrapeQLCoach );

setInterval(() => {}, 100000);
